version: 2

seeds:
  - name: sports
    config:
      full_refresh: true
      column_types:
        counrty: varchar(20)
        sports: varchar(20)
        


  - name: pin_code
    config:
      full_refresh: true    

  - name: my_details
    config:
      full_refresh: true            #imp to create table
      
      column_types:
          id: number(20)
          name: varchar(20)

        

#csv file best for static data which changes in frequently 
#use command :: dbt seed  
## ONCE RUN THE DBT SEED ,it will create tabel in snowflake    
# o> dbt seed --select  my_details.csv